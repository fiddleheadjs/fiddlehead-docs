import vars from './vars.json';

export let {
    gray,
    color,
    space,
    border_radius,
    box_shadow,
    duration,
    z_index,
    dimension,
    line_height,
    font_size,
    font_weight,
    font_family,
    text_color,
    action_color,
    input_color,
    input_background,
    input_border_color,
    input_placeholder_color,
    input_box_shadow,
} = vars;
